#include <stdio.h>
/**
 * main - this is the function that performs the task
 * Return: always 0
 */
int main(void)
{
	char c;

	c = 'z';
	while
		(c >= 'a') {
			putchar(c);
			c--;
		}
	putchar('\n');
	return (0);
}
