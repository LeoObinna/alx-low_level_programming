#include "main.h"
/**
 * _isdigit - The checks for a digit (0 through 9)
 * @c: The int to be checked
 * Return: 1 if c is a digit, if otherwise 0
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
